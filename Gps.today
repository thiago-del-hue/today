<!DOCTYPE html>
<html lang="pt-BR">
<head>
     <meta charset="UTF-8">
     <meta name="viewport" content="width=device-width, initial-scale=1.0">
     <title>GPS</title>
     <style>
          body {
               transition: background-color 1s;
               font-family: Arial, sans-serif;
               color: rgb(17, 17, 202);
               text-align: center;
               padding-top: 70px;
          }
          button {
               padding: 10px 20px;
               font-size: 16px;
               margin-top: 20px;
               cursor: pointer;
               border-radius:5px;
          }
          @media(max-width:768px){
               body{ font-size:35px; padding-top: 50px;}
               button{ font-size: 14px;padding: 8px 16px;}
          }

     </style>
</head>
<body>
     <h1 id="título"> Olá Pessoas!</h1>
     <p>Clique no botão para mudar a cor da página de acordo com o horário local.</p>
     <button onclick="mudar()">Trocar Cor</button>

     <script>
          function mudar() {
               const hora= new Date().getHours();
               let cor;
               let mensagem;
               if(hora>=6 && hora < 12){
                    cor="#FFFF00"
                    mensagem= "Dia Animado!☀️";
               } else if(hora>=12 && hora < 18){
                    cor="#FF8C00";
                    mensagem="Tarde Alegre!🌇;";
               } else if(hora>=18 && hora < 23){
                    cor="#4B0082";
                    mensagem= "Noite Demoradaaa🌆";
               } else{
                    cor="#363636";
                    mensagem= " Hora de descansar!🌑";
               }
               document.body.style.backgroundColor= cor;
               document.getElementById("título").innerHTML= mensagem;

               }
     </script>
     
</body>
</html>
